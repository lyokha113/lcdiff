# Test Matrix

This file maps product behavior to proof. Platform-specific distribution rows
remain in progress until external runners or credentials provide evidence.

## Status Values

| Status | Meaning |
| --- | --- |
| planned | Accepted as intended behavior, not implemented |
| in_progress | Actively being built |
| implemented | Implemented and proof exists |
| changed | Contract changed after earlier implementation |
| retired | No longer part of the product contract |

## Matrix

| Story | Contract | Unit | Integration | E2E | Platform | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| jdiff-mvp-core | Validated open for JAR/ZIP/folder, lazy normalized-path read, duplicate normalized-path rejection, encrypted-entry rejection, CRC diff, strict signed and multi-release metadata, Zip64 metadata/write guard, path/text/constant-pool search, class/file staged atomic save, folder target copy, nested-archive diff and flattened merge | yes | yes | automated CLI smoke | Unix local | implemented | `rtk cargo test -p jdiff-core` (`28` core tests, including folder open/read, archive-to-folder compare, archive-to-folder staged commit with `<folder>.bak` backup, and nested-archive detection/extraction in `nested.rs` and `tests/core.rs`); `rtk cargo test -p jdiff-cli` (`3` CLI smoke tests for list/diff/read/search/copy `--backup`, text-content search, class constant-pool search, binary payload exclusion, and CLI empty-query rejection); backslash path read; shell-escaped pasted path validation; Windows home-dir fallback path validation; duplicate normalized-path rejection; encrypted-entry domain error; multi-release metadata; forced-small Zip64 fixture; incomplete-signature negative fixture; duplicate staged target replacement; directory-copy rejection; directory/timestamp preservation; source/target changed-on-disk rejection; read-only preflight regression; failed-backup pre-atomic-replace regression proving target untouched and temp cleanup; overwritten single-backup assertion |
| jdiff-desktop-shell | Tauri shell, React shadcn/ui Tailwind panels, async long-op IPC adapters, drag/drop, picker, path preflight with inline errors, Monaco diff whitespace toggle, detected text languages, binary size/SHA-256/CRC details plus hex fallback, pending badges, arrow/context-menu merge and unstage, dirty-close guard, filters, scoped structured streaming search results, hierarchical foldable file tree with per-node status, multi-tab diff workspace (10-tab LRU eviction, per-tab view-mode/preview state, middle-click close, confirm-before-close on new archive), nested-archive lazy tree expansion and merge, Config drawer, splash screen | yes | yes | build | macOS local | in_progress | Desktop state-machine tests for target lock, dirty switch, clear/unstage unlock, signed confirmation, resolved-path preflight, directory preview short-circuit, text-language mapping, binary SHA-256 preview, class-only bytecode UI affordance, non-class bytecode-view rejection, structured T2 search line/match-kind, T2 class constant-pool match-kind, empty-query search rejection, T3 per-entry decompile-error skip, same-directory sibling prefetch, sidecar worker preemptive cancel, and Linux Wayland runtime drop-hint detection; frontend invariant verifier covers shadcn/Tailwind config, component composition, shadcn ContextMenu tree-row merge actions, shadcn Resizable tree/editor panel composition, shadcn Dialog signed-save confirmation with session suppression, shadcn Tooltip action explanations, Tauri-only browser-preview guards, Single-mode file-drop targeting to the visible left panel, Linux Wayland Browse/path-input fallback hint, stale preview/search clearing after archive open, Single-mode search scoping to the visible left archive, search-result navigation revealing hidden tree rows and Monaco line matches, Single-mode merge-action/staged-save disabling, dirty mode-switch blocking, stale async preview suppression, stale T2 search completion suppression, stale deep-search event suppression, and deep-search busy-state cleanup; Playwright frontend render verifier covers visible shell controls, console-clean startup, mocked path-input validation error and clear-on-success, compare open/select/copy rendering of the pending target plus pending row badge and context-menu unstage clearing, tree-filter row visibility, scoped T2 search result click navigation, on-demand metadata-only class status, dirty mode-switch guard, clear-staged unlock into Single mode, signed-save session suppression, backup checkbox IPC propagation, Bytecode tab rendering through the disassemble IPC path, binary fallback details with SHA/CRC plus hex preview, and signed-JAR warning Dialog before confirmed Save anyway commits with a signed-invalidated message; `src-tauri/tauri.conf.json` has active bundling, app icon, and resources; `rtk npm run verify:all` covers frontend build plus release, packaging, CI, frontend invariant, frontend render, and docs verifiers; `rtk npm run tauri -- build --debug --bundles app`; macOS screenshots for empty shell, aligned loaded tree, CFR source diff, and ASM bytecode diff after Monaco dark-theme fix; `bash -n scripts/launch-linux-xwayland.sh`; `scripts/verify-linux-display-matrix.sh` records external Linux compositor/session Browse, path-input, OS-drop, and optional XWayland results as Markdown evidence; `scripts\verify-windows-platform.ps1` provides the external Windows atomic/signing runner for cargo tests, verifiers, sidecar smoke, NSIS/MSI build, and optional Authenticode signing; `scripts/verify-macos-distribution.sh` provides the external macOS distribution runner for aggregate verifiers, sidecar smoke, app build, inside-out signing, signed/final app xattr cleanup, target-specific JLINK/app/JRE Mach-O architecture guards, optional notarization, final app promotion, DMG packaging, post-DMG strict codesign, mounted-DMG app verification, and `platform-validation/macos-distribution-*.md` reporting, including `platform-validation/macos-distribution-aarch64-apple-darwin-20260605T133311Z.md`; ad-hoc inside-out signing with default WebView/JVM entitlements verified by `codesign -d --entitlements`, including deterministic signed-app output; `bash -n scripts/notarize-macos-app.sh`; `scripts/verify-remote-release-workflow.sh` dispatches or inspects the remote release workflow and records required artifact evidence; release workflow promotes signed macOS app over the final `jdiff.app` upload path and installs Playwright Chromium before `npm run verify:all`, using `--with-deps` on Linux; release workflow runs `npm run verify:all` preflight before sidecar assembly/build/package; `scripts/package-macos-dmg.sh` verified with `hdiutil create`, `hdiutil verify`, mounted `.app`, and `Applications` symlink; release workflow invariant verifier covers per-arch bundles, complete macOS secret guards, macOS sign/notarize/promote/dmg order, Windows secret guard, Windows PFX decode, timestamp fallback, and Windows signing before upload; packaging script invariant verifier covers Windows signtool SHA256/sign/verify, Windows platform runner gates, macOS inside-out signing, notarization, Gatekeeper assessment, and DMG staging; CI workflow invariant verifier covers macOS/Linux/Windows matrix, Java/Node/Rust setup, Playwright Chromium install with Linux --with-deps, Linux WebKit deps, quality gates, frontend invariant/render verifiers, sidecar assembly/smoke, and Tauri debug build; docs invariant verifier covers Markdown fences, developer verifier commands, frontend render evidence for staged copy, bytecode, binary fallback, and signed-save Dialog, and macOS sign/notarize/package order; `yq` parsed conditional macOS and Windows release signing workflow; `docs/PLATFORM_VALIDATION.md` defines external pass criteria; signed bundle startup with bundled JVM; CI matrix added but not run; `src/lib/tabs.test.ts` and `WorkspaceTabs.test.tsx` cover diff-tab upsert/LRU-evict/neighbor-pick, middle-click close, and nested-archive basename; `FileTree.test.tsx` and `src/lib/status.test.ts` cover hierarchical tree status rollup and filter visibility; `ConfigDrawer.test.tsx` covers the options drawer; `SplashScreen.test.tsx` covers the startup splash |
| jdiff-jvm-sidecar | CFR/Vineflower decompile, ASM bytecode, typed options boundary, timeout, retry, versioned LRU cache, jlink runtime | yes | yes | smoke | bundled Java 17 | implemented | `rtk cargo test --workspace` (`62` workspace tests across jdiff-core, jdiff-cli, and src-tauri, including canonical-path/options cache partition, LRU refresh, same-directory sibling prefetch, T3 per-entry decompile-error skip, sidecar cancel stop, binary-skip T2 regression, and nested-archive resolve/commit two levels deep); `rtk scripts/assemble-sidecar-resources.sh`; `rtk scripts/test-sidecar-smoke.sh` passed ping, CFR, inner/anonymous classes, ASM, Vineflower, and malformed fallback |

## Evidence Rules

- Unit proof covers pure domain and application rules.
- Integration proof covers backend enforcement, data integrity, provider
  behavior, jobs, or service contracts.
- E2E proof covers user-visible browser flows.
- Platform proof covers only shell, deployment, mobile, desktop, or runtime
  behavior that cannot be proven in lower layers.
- A story can be implemented without every proof column if the story packet
  explains why.
